# Benchmarks

Week 1 baseline command:

```bash
cmake --build --preset dev-debug --target benchmark-reference
```

Output must include:

`Reference: <N>-core <OS> -- target <40% CPU / <3 GB for 1k agents`
